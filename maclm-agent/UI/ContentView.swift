import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("maclm-agent")
            .frame(
                minWidth: 480,
                minHeight: 320
            )
    }
}
